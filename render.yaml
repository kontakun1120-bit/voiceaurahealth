services:
  - type: web
    name: voiceaurahealth-www
    env: python
    plan: free

    runtime: python-3.12.3

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT