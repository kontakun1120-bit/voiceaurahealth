services:
  - type: web
    name: voiceaurahealth-mini_app
    env: python
    plan: free

    runtime: python-3.12.3

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app_web:app
