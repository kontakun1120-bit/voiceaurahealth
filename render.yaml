services:
  - type: web
    name: voiceaurahealth-city_app
    env: python
    plan: free

    runtime: python-3.12.3

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn -w 2 -k gthread --threads 4 app_web:app
