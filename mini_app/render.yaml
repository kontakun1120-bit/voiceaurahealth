services:
  - type: web
    name: voiceaurahealth-api
    env: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn mini_app.core.app_web:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.10
